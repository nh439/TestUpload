@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    int res = ViewBag.res;
}

@if (res == 1)
{
    <script>
        Swal.fire({
            title: 'Congulation',
            text: "Change User Password Success",
            icon: 'Success',
            showCancelButton: false,
           // confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: '<a href ="/" class="btn btn-primary">OK</a>'
        }).then((result) => {
            if (result.isConfirmed) {
              //  window.location.href("/");
            }
        })
    </script>
}
else if (res == 2)
{
    <script>
        Swal.fire({
            title: 'Oops! Change Password Unsuccessful',
            text: "Old Password Incorrect",
            icon: 'error',
            showCancelButton: false,
            //confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: '<a href ="/user/Changepassword" class="btn btn-primary">OK</a>'
        }).then((result) => {
            if (result.isConfirmed) {
            //    window.location.href("/user/Changepassword");
            }
        })
    </script>
}
else
{
    <script>
        Swal.fire({
            title: 'Oops!..',
            text: "Change Password Unsuccessful",
            icon: 'error',
            showCancelButton: false,
           // confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: '<a href ="/user/Changepassword" class="btn btn-primary">OK</a>'
        }).then((result) => {
            if (result.isConfirmed) {
              //  window.location.href("/user/Changepassword");
            }
        })
    </script>
}
<meta http-equiv="refresh" content="10; url=/" />
